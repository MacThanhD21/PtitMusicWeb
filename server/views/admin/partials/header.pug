header.header 
    .container-fluid 
        .row.align-items-center 
            .col-3 
                .inner-logo 
                    a(href="/admin/dashboard") ADMIN
            .col-9 
                div(class="text-end")
                    a(
                        href=`${prefixAdmin}/accounts/edit/${user.id}`  
                        class="btn btn-primary btn-sm me-3"
                    ) #{user.fullName}
                    a(
                        href=`${prefixAdmin}/auth/logout`
                        class="btn btn-danger btn-sm"
                    ) Đăng xuất

